{
  "name": "couple-connect-tests",
  "version": "1.0.0",
  "scripts": {
    "test": "mocha tests/*.test.js",
    "test:watch": "mocha tests/*.test.js --watch"
  },
  "devDependencies": {
    "mocha": "^10.0.0",
    "chai": "^4.3.0",
    "socket.io-client": "^4.7.0"
  }
}